filter {
  if [type] == "apache_access_logs" {
    grok {
      match => { "message" => "%{COMBINEDAPACHELOG} %{QS:log_source}" }
    }

    date {
      match => ['timestamp', 'dd/MMM/yyyy:HH:mm:ss Z']
    }

    geoip {
      source => "clientip"
      target => "geoip"
    }
  }
}
    
